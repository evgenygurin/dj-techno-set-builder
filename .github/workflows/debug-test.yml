name: Debug Test

on:
  push:
    branches:
      - test/BPM-101-data-integrity-verification

permissions:
  contents: read

jobs:
  debug:
    name: Basic Debug
    runs-on: ubuntu-latest

    steps:
      - name: Simple test
        run: |
          echo "Simple test"
          echo "This should always pass"